//bubble sort simples, sem repeticoes
#include<stdio.h>

int main(void){
	int v[5] = {3,4,6,2,5};
	int i, temp;

	for(i=0; i<4; i++){
		if (v[i]>v[i+1]){
			temp = v[i];
			v[i] = v[i+1];
			v[i+1] = temp;
		}

	for(i=0; i<5; i++){
		printf ("%d", v[i]);
	}

	return 0;
}